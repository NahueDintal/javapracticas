[
  {
    "keywords": ["for", "bucle", "iterar", "loop"],
    "command": "for (inicialización; condición; incremento) { sentencias; }",
    "example": "for (int i = 0; i < 10; i++) { System.out.println(i); }",
    "description": "Bucle for clásico."
  },
  {
    "keywords": ["while", "bucle", "condición"],
    "command": "while (condición) { sentencias; }",
    "example": "int i = 0; while (i < 10) { System.out.println(i); i++; }",
    "description": "Bucle while."
  },
  {
    "keywords": ["if", "condicional", "si"],
    "command": "if (condición) { sentencias; } else { sentencias; }",
    "example": "if (x > 0) { System.out.println(\"positivo\"); } else { System.out.println(\"negativo o cero\"); }",
    "description": "Estructura condicional if-else."
  }
]
